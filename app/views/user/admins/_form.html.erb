<%= form_with(model: user_admin) do |form| %>
  <% if user_admin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_admin.errors.count, "error") %> prohibited this user_admin from being saved:</h2>

      <ul>
        <% user_admin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
