<p style="color: green"><%= notice %></p>

<% content_for :title, "Admins" %>

<h1>Admins</h1>

<div id="user_admins">
  <% @user_admins.each do |user_admin| %>
    <%= render user_admin %>
    <p>
      <%= link_to "Show this admin", user_admin %>
    </p>
  <% end %>
</div>

<%= link_to "New admin", new_user_admin_path %>
